# Shared Schemas (`packages/schemas`)

Centralized, authoritative data contracts for domain entities, flight telemetry, camera models, depth maps, 3D meshes, and quality reports.

Contains:
- Python Pydantic V2 definitions in `python/`
- Generated TypeScript interfaces in `ts/`
